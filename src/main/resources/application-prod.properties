spring.datasource.url=jdbc:postgresql://ep-twilight-mud-agpszduc.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_HDtnbVB2FGh7
spring.datasource.driver-class-name=org.postgresql.Driver

# Use validate: tables must exist, Flyway runs first
spring.jpa.hibernate.ddl-auto=validate
# If you just want to avoid the error temporarily in dev:
# Hibernate will add the missing columns automatically.
# ?? Not recommended in production ? migrations are safer.
#spring.jpa.hibernate.ddl-auto=update

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Avoid Hibernate/Flyway circular dependency
#spring.jpa.defer-datasource-initialization=true

app.cors.allowed-origins=https://meal-planner-5x1zdfj7s-aurimas-projects-377e0745.vercel.app